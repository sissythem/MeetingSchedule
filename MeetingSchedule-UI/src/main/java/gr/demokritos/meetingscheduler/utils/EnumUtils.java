package gr.demokritos.meetingscheduler.utils;

public class EnumUtils {
    public enum LeftMenuSelection {
        MEMBERS, USERS, AVAILABILITIES, MEETINGS
    }

    public enum TopMenuSelection {
        PROFILE, HELP, LOGOUT
    }

    public enum MessageType {
        INFO, WARN, ERROR, SUCCESS
    }

    public enum Action {
        ADD, EDIT, REMOVE, VIEW
    }

    public enum PasswordChangeFrom {
        LOGIN, GRID, PROFILE
    }

}
